root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/ragpack"
  cmd = "bash build-dev.sh"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "testdata"]
  delay = 1000
  stop_on_error = true
  kill_delay = "2s"
  send_interrupt = true

[log]
  time = true

[screen]
  clear_on_rebuild = true
